#!/usr/bin/env zsh

# System Update
if [ $IS_ARCH_BASE = true ]; then
  $PACTOOL -Syyu $PAKKER_ALL
else
  sudo dnf update -y
fi

# Flatpak Update
flatpak update -v --noninteractive

# Zinit Self update
. "$HOME/.zinit/bin/zinit.zsh"
zinit self-update

# Zinit Plugin update
zinit update --parallel
