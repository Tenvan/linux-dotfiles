---
- name: DefineAppPackages
  vars:
    global_init:
      # graphics
      - blender
      - feh
      - gimp
      - gimp-help-de
      - imv
      - inkscape
      - krita
      - pinta

      # music
      - mpc
      - mpd
      - playerctl

      # optional application packages
      - aspell

      # Browser
      - chromium
      - google-chrome

      # remmina
      - remmina
      - freerdp

      # scanner setup
      #- xsane
      #- simple-scan
      #- skanlite
      #- colord-sane
      #- sane

      # tools
      - translate-shell

      # gimicks
      - cmatrix

    arch_based_init:
      - baobab-gtk3

      # Browser
      - vivaldi
      - vivaldi-ffmpeg-codecs
      - firefox-i18n-de
      - microsoft-edge-dev-bin

      # Office
      - libreoffice-fresh
      - libreoffice-fresh-de

      # jetbrains apps
      - jetbrains-toolbox

      # gimicks
      - hollywood
      - cmatrix-git

      # graphics
      - imagemagick

    rhel_based_init:
      - baobab

      # Browser
      - vivaldi-stable
      - firefox
      - firefox-langpacks

      # graphics
      - GraphicsMagick

      # Office
      - libreoffice
      - libreoffice-help-de

      # Communication
      # - teams

    manjaro_init:
      - imagemagick-full

    flatpak_init:
      # Communication
      - com.github.IsmaelMartinez.teams_for_linux

      # Media
      - com.spotify.Client
      - de.haeckerfelix.Shortwave

  include_tasks: install.yml
