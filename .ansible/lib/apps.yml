---
- name: Define App Packages
  vars:
    global_init:
      # graphics
      - gimp
      - inkscape


      
    fedora_init:
      # Browser
      - chromium
      # - google-chrome

      - gimp-help-de

      # tools
      - krusader
      - kdiff3
      - krename

      # remmina
      - remmina

    arch_based_init:
      - baobab-gtk3
      
      # remmina
      - freerdp

      # Browser
      - firefox-i18n-de

      # Office
      - libreoffice-fresh
      - libreoffice-fresh-de

      # jetbrains apps
      - jetbrains-toolbox

      # graphics
      - imagemagick

      # music
      - mpd

    rhel_based_init:
      - baobab

      # remmina
      - freerdp

      # Browser
      - microsoft-edge-stable
      - firefox
      - firefox-langpacks

      # graphics
      - GraphicsMagick

      # Office
      - libreoffice
      - libreoffice-help-de
      - libreoffice-langpack-de
      - libreoffice-icon-theme-papirus
      - libreoffice-gtk4
      - libreoffice-kf5
      - libreoffice-pdfimport

      # Communication
      # - teams

    manjaro_init:
      - imagemagick-full
    
    debian_based_init:
      # remmina
      - freerdp2-wayland
      - freerdp2-x11

    flatpak_init:
      # Communication
      - com.github.IsmaelMartinez.teams_for_linux

      # Media
      - com.spotify.Client
      - de.haeckerfelix.Shortwave
      - org.photoqt.PhotoQt

  include_tasks: install.yml
