---
- name: Install hpuld
  command: "{{ home_dir }}/.scripts/setup/uld/install.sh"

- name: Install nvm
  become: false
  shell: curl https://raw.githubusercontent.com/creationix/nvm/master/install.sh | bash


- name: Install ranger DevIcons
  become: false
  block:
    - name: Remove old plugins
      file:
        state: absent
        path: "{{ home_dir }}/.config/ranger/plugins/ranger_devicons"
    - name: Clone devicons
      git:
        repo: "https://github.com/alexanderjeurissen/ranger_devicons"
        clone: true
        depth: 1
        dest: "{{ home_dir }}/.config/ranger/plugins/ranger_devicons"

- name: Install micro plugins
  become: false
  shell: |
    micro -plugin install comment editorconfig joinLines quoter filemanager jump \
      detectindent go snippets crystal quickfix wc manipulator diff ftoptions \
      linter literate status monokai-dark gotham-colors nordcolors
    micro -plugin update
  register: my_output
  changed_when: my_output.rc != 0

- name: Write xinit-system file
  copy:
    dest: /etc/X11/xinit/xinitrc.d/user-xinitrc.sh
    mode: u+rwx,g+rx,o+rx
    content: |
      #!/usr/bin/sh
      if [ -f $HOME/.xinitrc ]; then
        . $HOME/.xinitrc
      fi
  when: is_rhel_based

- block:
  - name: Create a directory if it does not exist
    file:
      path: /etc/lightdm/lightdm.conf.d
      state: directory
      mode: u+rw,g+r,o+r    
    when: is_suse_based is false

  - name: Write lightdm config
    copy:
      dest: /etc/lightdm/lightdm.conf.d/settings.conf
      mode: u+rwx,g+rx,o+rx
      content: |
        [Seat:*]
        greeter-session=slick-greeter
        greeter-hide-users=false
        autologin-user={{ user_id }}
    when: is_suse_based is false

  - name: Write slick-greeter config (default lightdm greeter)
    copy:
      dest: /etc/lightdm/slick-greeter.conf
      mode: u+rw,g+r,o+r
      content: |
        [Greeter]
        theme-name={{ lookup('env', 'MAIN_THEME') }}
        icon-theme-name={{ lookup('env', 'ICON_THEME') }}
        cursor-theme-name={{ lookup('env', 'CURSOR_THEME') }}
        background=/usr/share/wallpapers/Materia-Dark/contents/images/3840x2160.png
        content-align=center
        activate-numlock=true
    when: is_suse_based is false

  - name: Write lightdm-gtk-greeter config
    copy:
      dest: /etc/lightdm/lightdm-gtk-greeter.conf
      mode: u+rw,g+r,o+r
      content: |
        [greeter]
        background = /usr/share/wallpapers/Materia-Dark/contents/images/3840x2160.png
        theme-name = {{ lookup('env', 'MAIN_THEME') }}
        icon-theme-name = {{ lookup('env', 'ICON_THEME') }}
        font-name = Sans 12
        position = 50%,center 50%,end
        xft-antialias = false
        xft-dpi = 
        xft-hintstyle = hintnone
    when: is_suse_based is false

- name: remove kwin kservices5 folder
  file:
    path: "{{ home_dir }}/.local/share/kservices5"
    state: absent

- name: remove krohnkite kwin script
  file:
    path: "{{ home_dir }}/.local/share/kwin/scripts/krohnkite"
    state: absent

- name: Create own xinit-compat script
  blockinfile:
    path: "/opt/xinit-compat"
    create: true
    marker: "# {mark} ANSIBLE MANAGED BLOCK xinit"
    block: |
      #!/bin/sh          
      # ANSIBLE MANAGED BLOCK xinit"
      exec ~/.xsession
    mode: u+rwx,g+rx,o+rx

- name: remove awesome session (fedora)
  file:
    path: /usr/share/xsessions/awesome-fedora.desktop
    state: absent

- name: Refresh icons on ArchLinux
  become: true
  command: gdk-pixbuf-query-loaders --update-cache
  when: is_arch_based

- name: refresh icons on RHEL
  become: true
  command: gdk-pixbuf-query-loaders-64 --update-cache
  when: is_rhel_based

- name: Add Printer Defs 
  become: true
  blockinfile:
    path: "/usr/share/hplip/data/models/models.dat"
    create: false
    marker: "# {mark} ANSIBLE MANAGED BLOCK HP MFP 178"
    block: |
      [hp_color_laser_mfp_178_179]
      align-type=0
      clean-type=0
      farbe-kal-typ=0
      kopier-typ=0
      eingebetteter-server-typ=1
      fax-typ=7
      fw-download=False
      bild=hp_farbig_laserjet_cp2025.png
      io-mfp-mode=1
      io-modus=1
      io-support=14
      job-storage=0
      zeilenvorschub-kal-typ=0
      model1=Terrible HP Farblaser-MFP 178nwg
      monitor-typ=0
      panel-check-type=0
      pcard-typ=0
      plugin=0
      plugin-grund=65
      Strom-Einstellungen=0
      pq-diag-typ=0
      r-typ=0
      r0-agent1-kind=4
      r0-agent1-sku=CE311A
      r0-agent1-typ=4
      r0-agent2-kind=4
      r0-agent2-sku=CE310A
      r0-agent2-typ=1
      r0-agent3-typ=4
      r0-agent3-sku=CE313A
      r0-agent3-typ=5
      r0-agent4-kind=4
      r0-agent4-sku=CE312A
      r0-agent4-typ=6
      scan-src=3
      scannen-typ=5
      status-batterie-check=0
      status-dynamische-zähler=0
      status-typ=10
      support-released=True
      unterstützung-subtype=219b2b
      support-type=2
      unterstützung-ver=3.13.11
      tech-class=Hbpl1
      familienklasse=PCLM-COLOR
      tech-subclass=Color
      tech-type=4
      usb-pid=332a
      usb-vid=3f0
      wifi-config=3
