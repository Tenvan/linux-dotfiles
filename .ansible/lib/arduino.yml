---
- name: Define Arduino Packages
  vars:
    global_init:
      # Microcontroler Develop
      - esptool

      # cnc tools
      - inkscape
      - blender
      # - lux
      # - luxcorerender-bin

    archlinux_based_init:
      # IDEs
      - arduino

      # Microcontroler Develop
      - platformio

  #    pip_init:
  #      - wheel
  #      - esphome

  ansible.builtin.include_tasks: install.yml
