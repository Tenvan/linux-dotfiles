---
- name: DefineDevelopmentPackages
  vars:
    global_init:
      - cargo
      - cmake
      - colorgcc
      - gcc
      - git
      - go
      - jq
      - make
      - npm
      - nuget
      - python-pip
      - ruby
      - rust
      - tk

    archlinux_based_init:
      - base-devel

      # DotNet/Mono Setup
      - dotnet-sdk
      - mono
      - mono-msbuild

    rhel_based_init:
      # DotNet/Mono Setup
      - mono-complete
      - dotnet-sdk-6.0
      - dotnet-sdk-7.0

      # missing perl moduls
      - perl-CPAN
      - perl-JSON-Any

    pip_init:
      - anybadge
      - boto3

  include_tasks: install.yml
