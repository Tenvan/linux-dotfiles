---
- name: DefineDevelopmentPackages
  vars:
    global_init:
      - cargo
      - cmake
      - gcc
      - git
      - golang      
      - jq
      - make
      - npm
      - ruby
      - tk

    archlinux_based_init:
      - base-devel
      - colorgcc
      - nuget
      - python-pip
      - python-pipx
      - rust

      # DotNet/Mono Setup
      - dotnet-sdk
      - mono
      - mono-msbuild

    rhel_based_init:
      - rust
      
      # DotNet/Mono Setup
      - dotnet-sdk-6.0
      - dotnet-sdk-7.0

      # missing python moduls
      - python3-pip
      - pipx

    debian_based_init:
      - rust-all
      
      # DotNet/Mono Setup
      # TODO Dotnet in Debian
      - dotnet-sdk-6.0
      - dotnet-sdk-7.0

      # missing python moduls
      - python3-pip
      - pipx

    fedora_init:
      - colorgcc
      - nuget
      - mono-complete

      # missing perl moduls
      - perl-CPAN
      - perl-JSON-Any

    pip_init:
      - anybadge
      - boto3

  include_tasks: install.yml
