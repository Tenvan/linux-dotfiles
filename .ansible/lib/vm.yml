---
- name: DefineVmPackages
  vars:
    global_init:
      - dnsmasq
      - ebtables
      - iptables-nft
      - libvirt
      - virt-manager

    archlinux_based_init:
      - bridge-utils
      - openbsd-netcat
      - ovmf
      - qemu-arch-extra
      - qemu-desktop
      - vde2
      - virtio-win

    fedora_init:
      - bridge-utils
      - edk2-ovmf
      - swtpm

  include_tasks: install.yml
