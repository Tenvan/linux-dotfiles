---
- name: DefineVmPackages
  vars:
    global_init:
      - dnsmasq
      - libvirt
      - virt-manager

    archlinux_based_init:
      - bridge-utils
      # - openbsd-netcat
      - ovmf
      - qemu-arch-extra
      - qemu-desktop
      - vde2
      - virtio-win

    rhel_based_init:
      - ebtables
      - iptables-nft
    
    fedora_init:
      - @virtualization
      - bridge-utils
      - edk2-ovmf
      - swtpm

  include_tasks: install.yml
