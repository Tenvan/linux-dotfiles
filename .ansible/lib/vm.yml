---
- name: DefineVmPackages
  vars:
    global_init:
      - bridge-utils
      - dnsmasq
      - ebtables
      - iptables-nft
      - libvirt
      - virt-manager

    archlinux_based_init:
      - openbsd-netcat
      - ovmf
      - qemu-arch-extra
      - qemu-desktop
      - vde2
      - virtio-win

  include_tasks: install.yml
