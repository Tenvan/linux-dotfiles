# code: language=ansible
---
- name: DefineArduinoPackages
  vars:
    global_init:
      # Microcontroler Develop
      - esptool

      # cnc tools
      - inkscape
      - blender
      # - lux
      # - luxcorerender-bin

    archlinux_based_init:
      # IDEs
      - arduino

      # Microcontroler Develop
      - platformio

    fedora_based_init:

    pip_init:
      - wheel
      - esphome

  include_tasks: lib/install.yml
